# Sidecar-only dependencies — NEVER add these to the main requirements.txt.
# The main application runtime uses only the Python standard library; reranking
# runs in this isolated optional environment (own venv).
torch>=2.6,<3
transformers>=4.51,<5

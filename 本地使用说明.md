# 本地完整版使用说明

这个目录是可直接运行、可重新建库的完整本地版本，包含教材、知识库和 Windows 运行包。教材和生成数据仅保存在这里，不要把整个目录上传到公开仓库。

当前版本合并了两条开发线的全部成果：检索质量调优（bge-m3 向量、标题加权、LLM 摘要）、答案缓存、telemetry，以及追问多轮对话、引用核验与重编号、安全加固、材料预算等。

## 最快启动

双击根目录中的 `启动本地版.bat`。

启动器会检查 Ollama；如果 Ollama 已安装但服务未运行，会尝试启动服务，然后打开：

`runtime\LocalDatabaseQA\LocalDatabaseQA.exe`

浏览器自动打开 `http://127.0.0.1:8765`，本机自动完成配对，无需输入配对码。

运行包内嵌了打包当时的知识库快照。**修改教材或重新建库后需要重新打包**（见下文"更新一键运行包"），运行版才会包含新数据。

## 界面与问法

### 支持的问法

系统按意图路由，不同问法走不同通路：

| 问法示例 | 行为 |
|---|---|
| 这本书有哪些章节？ | 直接返回识别出的章节目录（不调用模型） |
| 介绍下这本书 / 这本书主要讲了什么 | 基于全书摘要作答（支持 LLM 预生成摘要） |
| 第三章主要讲什么 / 第十一章主要讲什么 | 基于该章摘要作答，中文数字章节号支持到九十九 |
| 多径衰落在哪一页 / 扩频技术在哪个章节 | 返回位置和页码（不调用模型） |
| GSM 和 WCDMA 有什么区别 | 按比较双方分别检索证据后对比 |
| LTE 为什么采用 OFDMA？ | 常规检索问答 |

### 追问（多轮对话）

回答区是会话式记录，可以直接接着问：

- "那第二章呢？"、"介绍下第二层"、"那它有什么缺点？"、"缺点呢？"——系统会把上一问与追问合并用于路由和检索，并把最近 3 轮对话交给模型理解指代；
- 回答仍然只依据本轮检索到的材料，历史只用于理解"它/第二层"这类指代；
- "清空会话"按钮一键回到初始状态；带追问上下文的请求不使用答案缓存。

回答中的 `[1]` 引用标记可以点击，会跳转到右侧证据面板对应的原文卡片并高亮；答案按 markdown 渲染（标题、列表、加粗、代码）。

### 离题与材料不足

教材范围外的问题（天气、菜谱等）会直接拒答；材料不足以支撑结论时，回答会明确说明材料未涵盖，不会编造，也不会引入教材之外的理论框架。

## 数据位置

- 原始教材：`data\pdf\`（当前为《移动通信 (李兆玉)》）
- 提取文本：`data\raw\book.txt`
- 当前知识库（单一运行时真相源）：`data\library\documents.sqlite3`（schema 5，bge-m3 1024 维；含 5 个示例文档与《移动通信 (李兆玉)》教材，已通过 Phase D.1.1 迁移合并，embeddings 原样复用未重嵌入）
- 历史教材库：`data\library\textbooks.sqlite3`（schema 4，迁移后保留不删除；若两个库都含数据且缺迁移证据，程序会显式报错并给出 `scripts/migrate_legacy_library.py` 修复指引，绝不静默忽略任何一边）
- 一键运行包：`runtime\LocalDatabaseQA\`
- 调优数据：`data\logs\qa_log.jsonl`（每次回答一行，见"日志与调优数据"）

## 模型

运行需要 Ollama 与两个模型：

```powershell
ollama pull qwen2.5:7b      # 回答模型
ollama pull bge-m3          # 向量模型（当前知识库使用；nomic-embed-text 为兼容默认值）
```

## 配置项

通过环境变量配置（程序不自动加载 `.env` 文件，模板见 `.env.example`）：

| 变量 | 默认 | 说明 |
|---|---|---|
| `QA_OLLAMA_URL` | `http://localhost:11434` | Ollama 服务地址 |
| `QA_ANSWER_MODEL` | `qwen2.5:7b` | 回答模型 |
| `QA_EMBEDDING_MODEL` | `nomic-embed-text` | 向量模型（当前库为 bge-m3） |
| `QA_ANSWER_CONTEXT_WINDOW` | `8192` | 上下文窗口；材料按窗口预算自动裁剪 |
| `QA_ANSWER_NUM_PREDICT` | `800` | 回答最大 token |
| `QA_DESKTOP_HOST` | `127.0.0.1` | 设为 `0.0.0.0` 允许局域网访问 |
| `QA_DESKTOP_PORT` | `8765` | 端口（被占用时自动换随机端口） |
| `QA_PAIRING_CODE` | 随机 | 固定六位配对码 |
| `QA_LOG_FILE` | 关闭 | 设为路径（如 `qa.log`）时记录每次问答的日志 |
| `QA_TELEMETRY` | `1` | 设为 `0` 关闭 telemetry JSONL 输出 |
| `LIBRARY_IMPORT_ROOTS` | 空 | Web 路径导入/改链允许的目录（`;` 分隔）。未配置时 Web 路径导入默认禁用（CLI 导入不受限制） |
| `QA_PDF_MAX_PAGES` | `2000` | 单个 PDF 允许的最大页数（超出类型化拒绝） |
| `QA_PDF_MAX_EXTRACTED_CHARS` | `20971520` | 单个 PDF 提取文本的最大字符数（超出类型化拒绝） |

## 重新提取并建库

```powershell
python -m pip install -r requirements-ingest.txt
python rebuild_all.py
```

`rebuild_all.py` 会依次执行 PDF/OCR 提取 → 建库（含 `--llm-summaries`，用回答模型为每章生成摘要）。只重建知识库时：

```powershell
python -X utf8 scripts/build_library.py --model bge-m3 --llm-summaries
```

要点：

- 建库先写临时文件，校验完整性、片段数和向量数后才原子替换正式库，中途失败不会破坏旧库；
- 片段 ID 与内容、页码绑定（不依赖全局序号），再次构建时未变化的片段直接复用向量，只重算改动部分；
- 换嵌入模型等于换向量空间，需要全量重嵌（置信度门限会按库记录的模型自动切换）。

## 更新一键运行包

```powershell
python -m pip install -r requirements-build.txt
powershell -ExecutionPolicy Bypass -File .\build_windows.ps1 -SkipIndex
```

产物更新到 `runtime\LocalDatabaseQA\`，临时目录自动删除。

`-SkipIndex` 只影响是否重建知识库：不带时会先重建（自动使用 `QA_EMBEDDING_MODEL` 环境变量指定的模型，默认 bge-m3，并带 `--llm-summaries` 生成 LLM 摘要；片段 ID 稳定，未变化的向量直接复用，但需要 Ollama 在线）；日常只改了代码、没动教材时加 `-SkipIndex` 更快。

打包前脚本会自动跑全部单元测试；如果旧版 exe 还在运行，会因 DLL 被占用而失败，先退出程序再打包。

## 日志与调优数据

- `data\logs\qa_log.jsonl`：telemetry（默认开启），每次回答记录时间、问题、路由、检索模式、置信度、是否离题、引用是否核验通过、耗时——用于离线分析误拒/误放和路由分布；
- **exe 模式的 telemetry 写在运行包内部**：`runtime\LocalDatabaseQA\_internal\data\logs\qa_log.jsonl`，重新打包会清空它，需要长期留存请定期复制出来；源码模式（`python -m desktop`）写在项目的 `data\logs\`；
- `QA_LOG_FILE`：可选的服务端日志（人读格式），记录内容与 telemetry 类似；
- 置信度门限按嵌入模型注册表自动切换（见 `core/library_store.py` 的 `MODEL_DENSE_GATES`），换模型后可依据 telemetry 数据校准。

## 验证与测试

不需要教材和 Ollama：

```powershell
python -B -m unittest discover -v
python -B -m compileall -q core desktop scripts tests
node --check web/app.js
node --check web/library.js
node --check web/markdown.js
python -m ruff check core desktop scripts tests
```

已有真实知识库和 Ollama 时：

```powershell
python -X utf8 scripts/eval_recall.py        # golden set 召回率/范围门控/路由回归门禁
python -X utf8 scripts/regression_test.py    # 真实检索回归
```

## 手机访问

手机和电脑连接同一个可信局域网后，从源码启动：

```powershell
$env:QA_DESKTOP_HOST="0.0.0.0"
python -m desktop
```

局域网设备在"连接设置"里输入电脑显示的六位配对码连接；配对失败会限速，会话令牌 12 小时有效。服务做了 Host 校验（防 DNS rebinding）且未开放 CORS，**不要把该 HTTP 服务直接暴露到公网**。手机端复制按钮在非 HTTPS 环境会自动走兼容方案。
